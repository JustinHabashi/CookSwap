import ubc from '../map.png'

export const NearbySwaps = (props) => (
    <div>
        <header className="App-pages">
        <b>Nearby Swaps</b>

        <div>
        <p> <img src={ubc} className="images" alt="logo" /> </p>
        </div> 
        <p> Imagine an interactive map where you can view potential CookSwaps nearby! </p>
        </header>
    </div>
)